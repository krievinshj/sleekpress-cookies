# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.sublime-*

# Build deps (the built bundle in assets/admin/dist IS committed)
node_modules/
*.log

# Note: vendor/sleekpress-ui/ is the vendored shared UI kit — it is committed.
